// un numero es par o no?
function esPar(numero) {
    if (numero % 2 === 0) {
      console.log(`${numero} es par.`);
    } else {
      console.log(`${numero} es impar.`);
    }
  }
  esPar(-1); // Cambia este valor para probar
  